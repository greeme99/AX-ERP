# 미설정 시 기본값: sqlite:///app/erp.db (별도 설정 불필요)
# PostgreSQL로 전환하려면 아래 주석을 해제하고 docker-compose up -d 로 로컬 Postgres를 띄운 뒤 사용:
# (호스트 포트는 5433 — 로컬에 이미 설치된 다른 Postgres(5432)와 충돌을 피하기 위함)
# DATABASE_URL=postgresql+psycopg2://erp:erp_local_dev@127.0.0.1:5433/standard_erp
